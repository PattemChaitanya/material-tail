{
  "name": "material-tail-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:cli": "npm run build --workspace=packages/cli",
    "build:registry": "npm run build --workspace=packages/registry",
    "start:showcase": "npm start --workspace=apps/showcase"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
